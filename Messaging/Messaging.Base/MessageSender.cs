using IMessaging;
using System;
using System.Threading;
using System.Threading.Tasks;

namespace Messaging.Base {
	public class MessageSender {
		/*private Type ReturnType;
		public Action<ResponseMessage> Action { get; private set; }

		public MessageSender() {
		public void SetAction<T>(Action<T> pAction) where T : ResponseMessage {
			ReturnType = typeof(T);
			Action = pAction as Action<ResponseMessage>;
		}
		public void Run() {
		}*/
	}
}
